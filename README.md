> ⚠️ **Moved to the monorepo.**
> This module now lives in [`opstimus/terraform-modules`](https://github.com/opstimus/terraform-modules) at `modules/oci-redis`.
>
> ```hcl
> source = "git::https://github.com/opstimus/terraform-modules.git//modules/oci-redis?ref=oci-redis/v1.0.0"
> ```
>
> This repository remains for existing consumers; new development happens in the monorepo.

# terraform-oci-redis
terraform-oci-redis
